#pragma once

// Networking configuration for cp2077-coop.
// All logic assumes the dedicated server is authoritative.
// Host migration may be considered in the future.

namespace CoopNet
{
    constexpr bool kDedicatedAuthority = true;
}
